{% if zapisnik_list %}
    <ul>
        {% for zapisnik in zapisnik_list %}
        <li><a href="{% url 'zapisnici:zapisnik' zapisnik.id %}">{{ zapisnik.zap_sadrzaj }}</a></li>
        {% endfor %}
    </ul>
{% else %}
    <p>Trenutno nema zapisnika.</p>
{% endif %}

    
